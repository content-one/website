
    <div class="container my-5">
        <div class="row">
            <!-- Main Content: 3/4 width on desktop, appears first on mobile -->
            <div class="col-12 col-lg-9 order-1 order-lg-1">
                <div class="main-content mr-6">
                    <div class="content"></div>
                    <!-- Add more product details here -->
                </div>
            </div>

            <!-- Sidebar: 1/4 width on desktop, appears second on mobile -->
            <div class="col-12 col-lg-3 order-2 order-lg-2">
            
                <div class="collapse d-md-block" id="collapseAccountMenu">
                    <h3 class="font-light">Product Tour</h3>
                    <ul class="nav flex-column nav-account">
                        {{each product_sections as section order by section.sort_order  }}
                            <li class="nav-item {{$active}}">
                                <span class="ms-2"><strong>{{section.section_name}}</strong></span>
                            </li>
                            {{each product_tour_pages as tour_page where product_section = '{section.zuid}'}}
                            {{$active = ""}}
                            {{if '{tour_page.path_part}' == '{request.pathPart(2)}' }}
                            {{$active = active at}}
                            {{/if}}
                            <li class="nav-item {{$active}}">
                                <a class="nav-link {{$active}}" href="{{tour_page.getURL()}}" title="{{tour_page.title}}">
                                    <i class="align-bottom bx bx-home"></i>
                                    <span class="ms-2">{{tour_page.seo_link_title}}</span>
                                </a>
                            </li>
                            {{/each}}
                        {{/each}}
                    </ul>
                </div>

            </div>
        </div>
    </div>
</section>